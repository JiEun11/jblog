<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="category">
	<insert id="insert" parameterType="categoryvo">
		<![CDATA[
			INSERT INTO category VALUES(null, #{name }, #{description }, #{blogId } )
		]]>
	</insert>
	
	<select id="findAllById" parameterType="string" resultType="categoryvo">
		<![CDATA[
			SELECT no,name,description,blog_id AS blogId FROM category WHERE blog_id=#{blogId } ORDER BY name ASC;
		]]>
	</select>
</mapper>
